> **DEPRECATED (2026-06-04).** `/fleet-generate` is retired. Its value — a stack-tailored,
> specialist roster — is now derived automatically inside `/poc-builder` (the FRAME workflow
> `poc-frame` maps the Architecture Spec to specialists, auto-selects, announces, and proposes a
> new specialist when nothing matches).
>
> **Use `/poc-builder {ClientName}` instead.** See `_System/skills/poc-builder.md` and
> `Knowledge/Practice/AI-Engineering/docs/PoC-Builder-Guide.md`.
>
> (Standalone fleet generation for an EXISTING non-poc-builder repo is no longer supported as a
> separate command; if you need it, run `/poc-builder` and stop after Gate 1 — the roster is in the
> FRAME return.)
